[project]
name = "cec-data-clearn-and-combine"
version = "0.1.0"
description = "A package to clean and combine CEC data."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=2.3.3",
]
